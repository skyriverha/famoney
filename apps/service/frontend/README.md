# FaMoney Frontend

FaMoney의 Next.js 14 기반 프론트엔드 애플리케이션입니다.

## 기술 스택

- **Framework**: Next.js 14 (App Router)
- **Language**: TypeScript 5.x
- **UI Library**: Material-UI (MUI) 7.x
- **State Management**: Zustand
- **Styling**: Tailwind CSS + MUI sx prop
- **Icons**: @mui/icons-material

## 프로젝트 구조

```
apps/service/frontend/
├── app/
│   ├── (auth)/              # 인증 관련 페이지
│   │   ├── login/           # 로그인
│   │   ├── signup/          # 회원가입
│   │   └── layout.tsx
│   ├── (dashboard)/         # 대시보드 페이지
│   │   ├── ledgers/         # 원장 목록
│   │   ├── expenses/        # 지출 기록
│   │   ├── statistics/      # 통계
│   │   ├── profile/         # 프로필 설정
│   │   └── layout.tsx
│   ├── layout.tsx           # 루트 레이아웃
│   ├── page.tsx             # 랜딩 페이지
│   └── globals.css
├── components/
│   ├── layout/              # 레이아웃 컴포넌트
│   │   ├── BottomNavigation.tsx
│   │   ├── TopAppBar.tsx
│   │   └── SideDrawer.tsx
│   ├── ui/                  # 공통 UI 컴포넌트
│   ├── expense/             # 지출 관련 컴포넌트
│   └── ledger/              # 원장 관련 컴포넌트
├── lib/
│   ├── theme.ts             # MUI 테마 설정
│   ├── ThemeRegistry.tsx    # 테마 프로바이더
│   └── utils.ts             # 유틸리티 함수
├── store/
│   └── authStore.ts         # Zustand 인증 스토어
└── types/
    └── index.ts             # TypeScript 타입 정의
```

## 시작하기

### 개발 서버 실행

```bash
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000)을 열어 확인하세요.

### 빌드

```bash
npm run build
```

### 프로덕션 서버 실행

```bash
npm start
```

## 구현된 기능 (Phase 1)

### ✅ 완료된 페이지

- **랜딩 페이지** (`/`)
  - Hero 섹션
  - 주요 기능 소개 (투명성, 편의성, 협업)
  - 사용 사례 (가족 가계부, 모임 회계, 룸메이트)
  - CTA 버튼

- **로그인** (`/login`)
  - 이메일/비밀번호 입력
  - 비밀번호 표시/숨김 토글
  - 자동 로그인 체크박스
  - 폼 검증
  - 더미 로그인 (Mock)

- **회원가입** (`/signup`)
  - 이메일, 비밀번호, 이름 입력
  - 프로필 이미지 업로드 (선택)
  - 비밀번호 강도 표시
  - 비밀번호 확인
  - 폼 검증

- **대시보드** (`/ledgers`)
  - 사용자 인사말
  - 빠른 통계 (원장 수, 이번 달 지출, 최근 활동)
  - 원장 목록 (Empty State)
  - 하단 네비게이션

### 🚧 준비 중인 기능

- 원장 생성/수정/삭제
- 원장 상세 페이지
- 지출 기록 CRUD
- 멤버 관리
- 통계 및 차트
- 프로필 설정

## 디자인 시스템

### 색상 팔레트

- **Primary**: Blue 600 (#2563eb)
- **Secondary**: Green 600 (#10b981)
- **Error**: Red 500 (#ef4444)
- **Warning**: Amber 500 (#f59e0b)
- **Success**: Green 600 (#10b981)

### 타이포그래피

- **H1**: 2rem (32px), Bold
- **H2**: 1.5rem (24px), Bold
- **H3**: 1.25rem (20px), Semibold
- **Body1**: 1rem (16px)
- **Body2**: 0.875rem (14px)

### 간격

- 기본 단위: 4px
- Border Radius: 8px (sm), 12px (md), 16px (lg)

## 상태 관리

### Zustand Store

- **authStore**: 사용자 인증 상태 관리
  - `user`: 현재 로그인한 사용자
  - `token`: JWT 토큰
  - `setUser()`: 사용자 설정
  - `logout()`: 로그아웃
  - `isAuthenticated()`: 인증 상태 확인

## 모바일 최적화

- 반응형 디자인 (Mobile-First)
- 하단 네비게이션 바 (56px)
- Safe area 지원 (iOS notch)
- 터치 친화적 UI (최소 48px 터치 타겟)

## 개발 가이드

### 새 페이지 추가

1. `app/` 디렉토리에 라우트 생성
2. 필요한 경우 레이아웃 그룹 사용 `(auth)`, `(dashboard)`
3. MUI 컴포넌트 사용
4. TypeScript 타입 정의 (`types/index.ts`)

### 새 컴포넌트 추가

1. `components/` 디렉토리에 카테고리별로 생성
2. `'use client'` 지시어 추가 (상호작용 필요시)
3. Props 타입 정의
4. MUI의 `sx` prop 사용하여 스타일링

### 상태 관리

- 전역 상태: Zustand store 사용
- 서버 상태: React Query 사용 (추후 API 연동시)
- 로컬 상태: useState 사용

## 참고

- Figma 디자인: `docs/reference-source/FaMoney Mobile UI Design/`
- PRD: `docs/PRD.md`
- OpenAPI 스펙: `specs/openapi.yaml`
