{
  "name": "@famoney/api-client",
  "version": "0.0.1",
  "private": true,
  "description": "OpenAPI 기반 TypeScript API 클라이언트",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "openapi-generator-cli generate -i ../../specs/openapi.yaml -g typescript-fetch -o ./src/generated --additional-properties=supportsES6=true,typescriptThreePlus=true",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.15.3",
    "typescript": "^5.7.2"
  }
}
